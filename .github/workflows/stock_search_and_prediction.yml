name: Stock Search and Prediction Test

on:
  push:
    branches:
      - testing  
      - backend 
      - main 

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up cache
      uses: actions/cache@v4  
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.11'  # 3.x does latest version in 3

    - name: Install dependencies # to ensure dependencies are up to date run pip freeze > requirements.txt
      run: |
        python -m pip install --upgrade pip
        pip install --upgrade setuptools wheel
        pip install -r requirements.txt

    - name: Run tests
      run:  python -m unittest #try npm run or python -m unittest discover

#if this doesnt work
# - name: Install dependencies
#       run: |
#         python -m pip install --upgrade pip
#         pip install setuptools==57.0.0 wheel==0.36.2
#         pip install -r requirements.txt